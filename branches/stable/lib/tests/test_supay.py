import unittest

class TestDaemon(unittest.TestCase):
    def test___init__(self):
        # daemon = Daemon(name, pid_dir, log, stdin, stdout, stderr)
        assert False # TODO: implement your test here

    def test_spawn_child(self):
        # daemon = Daemon(name, pid_dir, log, stdin, stdout, stderr)
        # self.assertEqual(expected, daemon.spawn_child())
        assert False # TODO: implement your test here

    def test_start(self):
        # daemon = Daemon(name, pid_dir, log, stdin, stdout, stderr)
        # self.assertEqual(expected, daemon.start(check_pid, verbose))
        assert False # TODO: implement your test here

    def test_status(self):
        # daemon = Daemon(name, pid_dir, log, stdin, stdout, stderr)
        # self.assertEqual(expected, daemon.status())
        assert False # TODO: implement your test here

    def test_stop(self):
        # daemon = Daemon(name, pid_dir, log, stdin, stdout, stderr)
        # self.assertEqual(expected, daemon.stop())
        assert False # TODO: implement your test here

if __name__ == '__main__':
    unittest.main()
